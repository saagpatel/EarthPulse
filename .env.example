# Environment variables
# Copy to .env and fill in values before running

# NODE_ENV=development

# Vite cache directory override (optional, defaults to node_modules/.vite)
# VITE_CACHE_DIR=node_modules/.vite
